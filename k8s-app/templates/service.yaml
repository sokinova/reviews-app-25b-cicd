apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.app_name }}-service"
spec:
  selector:
    app: {{ .Values.app_name }}
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  type: {{ .Values.serviceType }}