apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend-lb
  name: frontend-lb
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: frontend-deployment
  type: LoadBalancer
status:
  loadBalancer: {}
